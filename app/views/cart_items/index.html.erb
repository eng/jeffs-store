<% if @cart_items.any? %>
  <ul>
    <% @cart_items.each do |cart_item| %>
      <li><%= cart_item.product.name %> x <%= cart_item.qty %></li>
    <% end %>
  </ul>
<% else %>
  <p>Cart is empty.</p>
<% end %>